using UnityEngine;

public class SceneUI : UIBase
{
    public override void Init()
    {
        base.Init();

        // 씬 UI 특성에 맞는 초기화 로직 (예: UIManager에 씬 UI로 등록)
        // Managers.UI.SetCanvas(gameObject, false);
    }
}
